import "./style.css";

const Footer = ({finalText}) => {
   return (
    <footer className="footer">
        <p className="paragraph">{finalText}</p>
    </footer>
   )
        
   
};

export default Footer;
